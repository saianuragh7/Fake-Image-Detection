services:
  - type: web
    name: pixeltruth
    runtime: docker
    dockerfilePath: ./Dockerfile
    autoDeploy: false
    healthCheckPath: /health
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
